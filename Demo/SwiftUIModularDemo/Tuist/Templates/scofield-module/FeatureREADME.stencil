# Feature{{ name }}

Generated by `tuist scaffold scofield-module --name {{ name }}`.

## Generated files

- `Sources/{{ name }}View.swift`

## Next steps

1. Add a new target entry for `Feature{{ name }}` in `Project.swift`.
2. Add the route case you need in `Modules/AppCore/Sources/AppCore.swift`.
3. Import `Feature{{ name }}` from `Modules/AppNavigation/Sources/AppRouter.swift`.
4. Register the route with `WrappingController` and `{{ name }}View`.
